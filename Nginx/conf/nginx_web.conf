# 设置某个目录直接映射到Web浏览器,浏览器可以直接访问目录下的文件
server {

    # 设置浏览器访问地址,浏览器访问时为ip:port/web
    location /web {
        # 映射的服务器目录地址
        alias /data/nginx/web;
        # 自动索引目录中的文件
        autoindex on;
        # 关闭显示文件大小
        autoindex_exact_size off;
    }
}